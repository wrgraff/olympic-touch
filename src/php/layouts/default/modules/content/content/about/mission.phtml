<?php
/** @var umiTemplaterPHP|BaseExtension|PageExtension|UmiSettingsExtension $this */
/** @var array $variables */
$page = $this->getPage();

$title = trim($page->getValue(SiteContentPageAboutModel::field_mission_title));
$content = trim($page->getValue(SiteContentPageAboutModel::field_mission_content));
if($title || $content):?>
    <section class="page-main__text-block text-block text-block_mission">
        <?php if($title):?>
            <h2 class="text-block__heading"><?php echo $title;?></h2>
        <?php endif;?>

        <?php if($content):?>
            <div class="text-block__text">
                <?php echo $this->parseTplMacros($content, $page->getId());?>
            </div>
        <?php endif;?>
    </section>
<?php endif;?>